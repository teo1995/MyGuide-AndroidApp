<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hr.foi.myguide">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.androidtutorialpoint.mymapsappsdirection.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/my_guide_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Prijava"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PocetnaStranica" />
        <activity android:name=".Registracija" />
        <activity
            android:name=".AddTour"
            android:label="Tours"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MyTour"
            android:label="@string/title_activity_edit_tour"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TourDetails"
            android:label="@string/title_activity_tour_details"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditTour"
            android:label="@string/title_activity_edit_tour"
            android:theme="@style/AppTheme" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <!--<meta-data-->
            <!--android:name="com.google.android.geo.API_KEY"-->
            <!--android:value="@string/google_maps_key" />-->

        <activity
            android:name=".TourMap"
            android:label="@string/title_activity_tour_map" />
        <activity
            android:name=".Profile"
            android:label="@string/title_activity_edit_profile"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TourSchedule"
            android:label="@string/title_activity_tour_schedule"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ViewSchedule"
            android:label="@string/title_activity_view_schedule"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TourScheduleDetail"
            android:label="@string/title_activity_tour_schedule_detail"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AddReservation"
            android:label="@string/title_activity_add_reservation"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".AddFilter"
            android:label="@string/title_activity_add_filter"
            android:theme="@style/AppTheme" />
    </application>

</manifest>